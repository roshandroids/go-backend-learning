# Dart ↔ Go Living Reference

This grows as concepts are covered. Seed table (from the roadmap doc) below;
add rows only once you've actually implemented both sides in `concepts/`.

| Dart | Go | Important Difference | Where implemented |
|---|---|---|---|
| `class` | `struct` + methods | No inheritance; methods live outside the type with an explicit receiver | `concepts/01-fundamentals` |
