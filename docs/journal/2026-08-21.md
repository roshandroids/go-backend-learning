# 2026-08-21 — Full Scaffolding Pass

## Learned
Not new Go concepts this session — this was a scaffolding pass, not a
learning pass. Every concept folder (`01`–`10`) and every project
(`01-cli` through `04-flutter-go-chat`) now has a real `// TODO`-gapped
exercise, tests (`t.Skip(...)` until the gap is filled), a Dart
comparison, and a filled-in README. `./scripts/test.sh` is green
everywhere (skipped tests, no failures) — the scaffolding compiles and
runs, but none of the actual exercises are solved yet.

Repo housekeeping done alongside: fixed `ROADMAP.md`'s dangling doc
links, closed a real CI glob gap (`projects/04`'s `go_server` was never
being tested), added `scripts/test.sh` (no root `go.mod`, so this is
the one-command way to check everything), and laid out the full
stage/project plan with a "You Are Here" section.

Two new dependencies added, each with an explicit decision point rather
than picked unilaterally: `github.com/jackc/pgx/v5` (Postgres driver,
bumped `concepts/go.mod` to Go 1.25 as its floor) and
`github.com/gorilla/websocket` + `github.com/golang-jwt/jwt/v5` (WS +
JWT).

## Dart comparison
N/A this session — no single concept; see each module's own README for
its specific Dart↔Go comparison.

## What confused me
_N/A — scaffolding session, not a learning session._

## Exercise
None solved. Full list of open `// TODO` gaps, in suggested order, is
in `ROADMAP.md`'s "You Are Here" section — start with
`concepts/01-fundamentals`'s `Scale`/`ScaleInPlace` (queued since
2026-08-08, still open).

## Known issues to fix before/while resuming
- Brain CLI (`brain-page` skill's `bin/brain.mjs`) is missing — none of
  today's decisions are recorded in `brain/`, only in `ROADMAP.md` and
  here. CLAUDE.md requires brain writes go through that CLI; it wasn't
  available, so nothing was hand-edited into `brain/` as a workaround.
- `projects/04-flutter-go-chat`'s Hub is intentionally not wired yet —
  needs a working Hub copied in from `projects/03-chat-server` first.
- No real Postgres or Flutter available this session, so
  `concepts/09-postgresql`'s `TaskRepository` and
  `projects/04-flutter-go-chat`'s `MessageStore` are reference-only /
  in-memory for now.

## Commits
`chore(ci,docs)` through `feat(flutter-chat)` — 18 commits, see
`git log --oneline f7a6f97..HEAD`. Commit dates for the first 12 were
spread across 2026-08-17 through 2026-08-20 at the user's request
(rewritten history, same tree contents, verified via empty diff before
the branch pointer was moved); the rest are dated 2026-08-21 as they
actually happened.
