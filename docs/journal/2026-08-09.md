# 2026-08-09 — Repository Scaffold

## Learned
- Set up the `go-backend-learning` repo structure: concepts/ (shared module)
  vs projects/ (independent modules per app).
- Wrote the first real Go code: a `Point` struct with value vs pointer
  receivers, plus its Dart equivalent.

## Dart comparison
Dart objects are always reference types, so "does this mutate the
original" is never ambiguous the way it is with Go's receiver choice.

## What confused me
_Nothing yet -- first session._

## Exercise
Completed the Stage 1 Point struct exercise (Level 2 -- Complete);
Scale/ScaleInPlace variant left as a TODO for next session.

## Commit
`chore: scaffold repository structure`
