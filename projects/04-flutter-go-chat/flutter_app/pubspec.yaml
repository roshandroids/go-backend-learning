name: flutter_go_chat
description: Flutter client for the Flutter+Go real-time chat project (Project 7).
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^2.4.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
