// Command cli is Project 1: a CLI tool that reads a CSV of transactions
// and outputs a summary. Deliberately left as a stub -- build this
// yourself per the roadmap's Project 1 spec before consulting the
// mentorship protocol for hints.
package main

import "fmt"

func main() {
	fmt.Println("TODO: parse args, read CSV, compute summary, print output")
}
